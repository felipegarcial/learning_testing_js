const calculator = {
	sum: (a, b) => {
		return a + b;
	},
	substract: (a, b) => {
		return a - b;
	},

	multiplay: (a, b) => {
		return a * b;
	},

	divide: (a, b) => {
		return a / b;
	},
};

export default calculator;
